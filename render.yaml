services:
  - type: web
    name: twitter-sentiment-analysis
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn pr:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
